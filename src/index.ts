/** Host half: the row exists so dsh-client-modules can discover the browser manifest. */
export const name = 'dsh-input-anywhere'

/** This plugin has no Host-side behavior. */
export function apply(): void {}
